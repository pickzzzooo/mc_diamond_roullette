command /opp:
	trigger:
		if player is op:
			execute console command "/deop %player%"
		else:
			execute console command "/op %player%"

command /var:
	trigger:
		set {만렙곡괭이} to tool of player

command /test:
	trigger:
		set {플레이어} to player
		give apple to {플레이어} 
